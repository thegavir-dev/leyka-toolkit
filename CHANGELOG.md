# Changelog

## 0.3.12

### Изменено

- **Описание плагина.** В plugin header добавлено упоминание поддержки БФ "Менора" со ссылкой на сайт фонда для отображения в админке WordPress.
- **Минимальные требования.** В plugin header добавлены требования WordPress `4.5+` и PHP `5.4+`; при активации теперь проверяется Leyka `3.20+`.
- **Лог.** Страница лога теперь показывает только последние 500 строк файла, чтобы не исчерпывать `memory_limit` при большом объёме.
- **`assign_donor_tag()`.** Удалён мёртвый блок поиска донора через post type `leyka_donor` — Leyka хранит доноров как WP-пользователей, этот путь никогда не срабатывал.
- **`handle_subscribe()`.** Удалена бесполезная строка `$donation->donor_subscribed = 1` — сохранение уже выполняется через `update_post_meta()` выше.
- **JS periodicity-хандлер.** `setTimeout(50)` заменён на `setTimeout(0)` — откладывает чтение `recurringInput.value` до следующего витка event loop, не добавляя произвольной задержки.

## 0.3.11

### Изменено

- **Порядок галок.** Рекуррентное согласие вставляется перед подпиской, чтобы итоговый порядок был: штатные галки Leyka, рекуррентное согласие, подписка.
- **Отступы галок.** Отступ `14px` теперь задается точечно для label рекуррентного согласия и подписки через собственные классы, без зависимости от `label:last-of-type`.

## 0.3.10

### Исправлено

- **Отступ перед рекуррентной галкой.** Добавлен `margin-top: 12px` для чекбокса внутри `.studioavp-lt-recurring-agree-inner`, чтобы интервал после предыдущей галки совпадал со штатными соглашениями Leyka.

## 0.3.9

### Изменено

- **Вставка рекуррентного согласия в native agreement `span`.** После переноса внутренняя обертка `.studioavp-lt-recurring-agree-inner` получает `display: contents`, чтобы ее `input` и `label` визуально выравнивались как прямые дети штатного `span` Leyka.
- **Проверка видимости.** Блокировка отправки учитывает явное значение `holder.style.display`, включая состояние `contents` для видимого рекуррентного согласия и `none` для скрытого.

### Исправлено

- **Выравнивание чекбокса.** Убрано смещение рекуррентной галки, вызванное вложенным inline-`span` внутри native agreement `span`.

## 0.3.8

### Изменено

- **DOM-стратегия рекуррентного согласия.** Внутренний блок чекбокса снова переносится внутрь native agreement `span`, как чекбокс подписки.
- **Проверка видимости.** Блокировка отправки теперь проверяет видимость по `agreeInput` и `holder`, что корректно для inline-вставки внутрь `span`.

### Исправлено

- **CSS отступов.** Удалены специальные компенсации отступов `.donor__oferta` и `:has()`, потому что рекуррентная галка больше не живет отдельным внешним `div`.

## 0.3.7

### Исправлено

- **Расположение подсказки.** Подсказка рекуррентного согласия теперь рендерится после submit-области формы.
- **Стиль подсказки.** Подсказка получила центрирование, размер и цвет вспомогательного текста Leyka.

## 0.3.6

### Исправлено

- **Блокировка кнопки пожертвования.** Добавлен `MutationObserver` для кнопки отправки: если Leyka снимает `disabled`, но обязательное рекуррентное согласие не отмечено, плагин сразу возвращает блокировку.
- **Резервная защита отправки.** Добавлен capture-перехват клика по кнопке, сохранен capture-перехват `submit` и добавлена защита программного `form.submit()`, чтобы форма не уходила без обязательного рекуррентного согласия.
- **Пересчет состояния Leyka.** После отметки галки или возврата к разовому платежу плагин инициирует пересчет формы через события на email-поле.

## 0.3.5

### Исправлено

- **Валидация рекуррентного согласия.** Для шаблонов Leyka с отключенной jQuery-валидацией добавлена собственная блокировка `submit`, если рекуррентное согласие обязательно, блок видим, а галка не отмечена.
- **Сообщение об ошибке.** При попытке отправки без обязательного рекуррентного согласия рядом с галкой выводится «Необходимо ваше согласие»; сообщение скрывается после отметки галки или переключения на разовый платеж.

## 0.3.4

### Исправлено

- **Вёрстка рекуррентного согласия.** Блок больше не вкладывается внутрь чужого `span`, а переносится как отдельная строка `.donor__oferta` в контейнер соглашений.
- **Обязательная галка.** Чекбокс рекуррентного согласия получил класс `leyka_agree`, чтобы штатная валидация Leyka учитывала динамический класс `required`.
- **CSS скрытия.** Глобальное скрытие больше не применяется к внутреннему `span`, только к блоку согласия и подсказке.

## 0.3.3

### Исправлено

- **Детектор рекуррентного режима.** Рекуррентное согласие теперь работает не только с чекбоксом `.leyka-recurring`, но и с актуальными переключателями `data-periodicity` и скрытым полем `leyka_recurring` в шаблонах `need-help` и `star`.

## 0.3.2

### Исправлено

- **Доступность рекуррентного согласия.** У внутреннего контейнера чекбокса удален лишний `aria-hidden`, чтобы скринридеры объявляли видимый чекбокс и его подпись.

## 0.3.1

### Добавлено

- **Метка донора для рекуррентного согласия.** После успешного пожертвования с отмеченным согласием на рекуррент донору назначается тег, по умолчанию `recurring-agree`.
- **Настройка `recurring_agree_tag`.** В секцию «Согласие на ежемесячное списание» добавлено поле метки донора.

### Улучшено

- **Асинхронное подтверждение платежа.** При переходе донации в `funded` плагин проверяет и подписку, и рекуррентное согласие, назначая соответствующие теги независимо друг от друга.

## 0.3.0

### Добавлено

- **Галка согласия на ежемесячное автоматическое списание.** Новый чекбокс показывается только при выбранном рекуррентном платеже и сохраняет факт согласия в meta донации `leyka_recurring_agreed`.
- **Настройки рекуррентного согласия.** В админке добавлены включение модуля, обязательность галки, текст чекбокса, текст подсказки и необязательная ссылка «Как отменить».

### Улучшено

- **Подключение фронтовых ресурсов.** Inline JS/CSS не подключаются, если отключены и чекбокс подписки, и модуль рекуррентного согласия.
- **JS для зоны оферт.** Селекторы учитывают оба служебных блока плагина, чтобы чекбоксы подписки и рекуррентного согласия не мешали друг другу в шаблоне `star`.

## 0.2.0

### Исправлено

- **Метка донора теперь присваивается корректно.** Корневая причина: Leyka создаёт WP-пользователя (роль «Донор») асинхронно — после хуков `leyka_new_donation_added` и даже после `transition_post_status`. Решение: назначение метки отложено на `shutdown`, когда Leyka уже завершила обработку. Кеш `get_user_by` сбрасывается через `wp_cache_delete()` перед поиском. Поддержаны оба сценария хранения доноров: WP users (основной) и `leyka_donor` post type (fallback).
- **Чекбокс подписки не отображался в шаблоне «star».** В star-шаблоне нет блока `.section__fields.agreements` — чекбоксы оферты/ПД находятся в `.donor__oferta span`. Добавлен дополнительный JS-селектор `.donor__oferta:not(.studioavp-leyka-addon-subscribe) span`. Исправлен fallback: `setProperty('display', 'block', 'important')` вместо `style.display = ''`, чтобы перебить CSS `!important`.

### Добавлено

- **Хук `on_donation_funded`.** Метка назначается при смене статуса донации на `funded` (callback от платёжного шлюза или ручная смена в админке). Приоритет 9999 — выполняется после всей логики Leyka.
- **Двойная стратегия поиска донора.** Сначала `get_user_by('email')` (WP users), затем `get_posts` по `leyka_donor` (если post type зарегистрирован).
- **Встроенный лог.** Пункт меню «Лог» с чекбоксом и кнопкой очистки. Файл `wp-content/uploads/leyka-toolkit/toolkit.log`, защищён `.htaccess`. Логирование по умолчанию выключено.

## 0.1.1

### Добавлено

- **Встроенный лог.** Новый пункт меню «Лог» с чекбоксом «Вести логирование» и кнопкой «Очистить лог». Лог хранится в `wp-content/uploads/leyka-toolkit/toolkit.log`, защищён `.htaccess`. Метод `handle_subscribe()` логирует каждый шаг: сохранение подписки, получение donor ID, проверку таксономии, создание и назначение метки. Логирование по умолчанию выключено.

## 0.1.0

### Исправлено

- **Метка донора не применялась.** Таксономия задана напрямую как `donors_tag` вместо обращения к константе `Leyka_Donor::DONORS_TAGS_TAXONOMY_NAME`, которая могла не совпадать. Убран несуществующий метод `get_donor()` — ID донора берётся из `$donation->donor_id` / `$donation->donor_user_id`. Термин создаётся автоматически через `wp_insert_term()`, если его ещё нет. Убран вызов `calculate_donor_metadata()`, который мог вызывать побочные ошибки. Добавлено логирование ошибок при создании и назначении метки.
- **Сохранение значения чекбокса подписки.** Метод `handle_subscribe()` сохраняет флаг подписки в штатный мета-ключ Leyka `leyka_donor_subscribed` и принудительно устанавливает `$donation->donor_subscribed = 1`, чтобы Leyka видела подписку сразу.
- **Проверка наличия Leyka при активации.** Если Leyka не установлена или не активна, плагин деактивируется и показывает сообщение об ошибке вместо того, чтобы работать вхолостую.
- **Проверка существования таксономии донора.** Перед `wp_set_object_terms()` добавлен вызов `taxonomy_exists()` — метка не записывается в несуществующую таксономию.
- **Зависимость фронт-скрипта от Leyka.** Inline-скрипт теперь зависит от `leyka-public` (если он зарегистрирован), что гарантирует корректный порядок выполнения при AJAX-рендере форм.

### Улучшено

- **Устойчивый DOM-селектор.** JS ищет целевой контейнер сначала по `[data-leyka-toolkit-target="subscribe"]`, затем по CSS-селектору `.section__fields.agreements .donor__oferta span` как fallback. Тема или плагин может пометить свой контейнер data-атрибутом — JS подхватит его вместо дефолтного селектора.
- **Фильтр для списка шаблонов.** Список поддерживаемых шаблонов вынесен в `apply_filters('leyka_toolkit_supported_templates', [...])`. Сторонний код может добавить свой шаблон без правки плагина.
- **Детерминированный field_id.** `wp_rand()` заменён на статический счётчик `$render_count` — стабильные ID при кешировании и в тестах.
- **Кеш настроек.** `settings_data()` читает `get_option()` один раз за запрос, результат хранится в `$settings_cache`. Кеш сбрасывается при сохранении настроек.

## 0.0.6

- Первый публичный релиз.
